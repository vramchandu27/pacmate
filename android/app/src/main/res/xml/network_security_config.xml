<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- No cleartext (HTTP) traffic permitted — all app traffic must use HTTPS -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
